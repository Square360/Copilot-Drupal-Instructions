# Exclude files from composer package (via export-ignore)
# These files won't be installed when the package is required
/docs export-ignore
/.gitattributes export-ignore
/.gitignore export-ignore
/CHANGELOG.md export-ignore
/COPILOT-PROMPT.md export-ignore

# Exclude project-specific files so they're not overwritten on composer update
# These files will only be created on initial install, never updated
/copilot-changelog.md export-ignore
/PROJECT-README.md export-ignore
